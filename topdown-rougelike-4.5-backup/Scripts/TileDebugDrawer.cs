using Godot;
using System;

public partial class TileDebugDrawer : Node2D
{

    public override void _Draw()
    {
       /* if (TurnManager.Instance.tileMap == null)
            return;

        foreach (Vector2I cell in TurnManager.Instance.tileMap.GetUsedCells(0))
        {
            Vector2 center = TurnManager.Instance.tileMap.ToGlobal(TurnManager.Instance.tileMap.MapToLocal(cell));
            DrawCircle(ToLocal(center), 2f, Colors.Red);
        }*/
    }
}
  

